# Copy this file to keystore.properties, then fill every value locally.
# storeFile may be an absolute path or a path relative to the project root.
# Never commit keystore.properties or the keystore itself.
storeFile=
storePassword=
keyAlias=
keyPassword=
